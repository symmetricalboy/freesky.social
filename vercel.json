{
  "rewrites": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "(?<subdomain>[^.]+)\\.(?<domain>.+)"
        }
      ],
      "destination": "/api/:path*"
    }
  ],
  "env": {
    "VERCEL_ENV": "@vercel/env/VERCEL_ENV"
  }
} 