{
  "rewrites": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "(?<subdomain>[^.]+)\\.(?<domain>.+)"
        }
      ],
      "destination": "/api/:path*"
    }
  ]
} 